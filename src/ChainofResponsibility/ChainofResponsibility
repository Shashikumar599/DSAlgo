Chain of responsibility is design pattern which helps in loose cupling where a request from client is
 served by many handlers we not to implicitly call anyone handler each handler process request and pass
 onto the next if required .
